# 내부 문서 · 로컬 전용
CLAUDE.md
docs/

# 빌드 산출물 (EC2 에서 매 배포마다 다시 만든다)
dist/

.DS_Store

# 포트폴리오 원본 이미지 (WebP 변환 전). 저장소에 넣지 않는다.
_src/

# tools/shrink_pdf.py 의존성 (pikepdf, pillow, pdfplumber) 용 venv
.venv/
